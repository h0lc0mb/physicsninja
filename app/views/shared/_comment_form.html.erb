<%= form_for([@question, @comment]) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="comment_field">
    <%= f.text_field :content, placeholder: "Add a comment...", rows: "2" %>
  </div>
  <%= f.submit "Submit comment", class: "comment_button" %>
<% end %>